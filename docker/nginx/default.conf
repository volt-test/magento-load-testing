# Wraps the nginx.conf.sample that ships in the Magento repo root.
upstream fastcgi_backend {
    server 127.0.0.1:9000;
}

server {
    listen 8080;
    server_name _;
    set $MAGE_ROOT /var/www/html;
    set $MAGE_MODE default;
    include /var/www/html/nginx.conf.sample;
}
